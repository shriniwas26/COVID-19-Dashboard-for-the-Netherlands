services:
  dashboard:
    # platform: linux/amd64
    restart: never
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dashboard
    ports:
      - 8080:8080
    environment:
      - DASH_DEBUG=1
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    volumes:
      - ./covid_dashboard_nl.py:/app/covid_dashboard_nl.py
      - ./update_data.py:/app/update_data.py
      - ./data:/app/data
      - ./assets:/app/assets
    command: uv run python covid_dashboard_nl.py
